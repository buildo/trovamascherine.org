import { style } from "treat";
import { red01 } from "../../variables";

export const errorText = style({
  color: red01,
});

export const errorBox = style({
  textAlign: "center",
  maxWidth: "350px",
});
