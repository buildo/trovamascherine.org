import { style } from "treat";

export const text = style({
  textDecoration: "none",
});

export const footer = style({
  padding: 0,
});

export const tosBlock = style({
  paddingTop: 20,
  borderTop: "1px solid #E7E7EA",
});
export const tosBlockLink = style({
  textAlign: "center",
  marginBottom: "10px",
});
