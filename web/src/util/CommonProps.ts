export interface CommonProps {
  /**
   * Optional id applied to the underlying DOM element
   */
  id?: string;
  /**
   * Optional class applied to the underlying DOM element
   */
  className?: string;
  /**
   * Optional width rule - components may have a default width set if this is not provided
   */
  width?: string;
}
